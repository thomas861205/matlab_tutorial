A = [3,-2,-1;-2,4,-2;-1,-2,4];
b = [-4; 0; 7];
aug = [A b];

for i = 1:size(A, 1) - 1
    % msg = sprintf('%d-th column', i);
    % disp(msg)
    
    for j = i + 1:size(A, 1)
        mul = aug(i, 1) / aug(j, 1);
        aug(j,:) 
    end
end